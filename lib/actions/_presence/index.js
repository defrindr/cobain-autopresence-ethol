const SendPresence = require("./send-presence");
const CheckPresence = require("./check-presence");
const HistoryPresence = require("./history-presence");

module.exports = {
    SendPresence,
    HistoryPresence,
    CheckPresence,
}